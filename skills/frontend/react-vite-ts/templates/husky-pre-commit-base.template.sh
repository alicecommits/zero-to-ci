# Managed by husky (see package.json "prepare": "husky" script).
# husky v9+ needs no sourcing boilerplate here — this file just needs
# to exist for `husky` to treat it as the pre-commit hook.
#
# Empty on purpose: this base file is proposed by husky-setup, whose
# only job is making sure .husky/pre-commit EXISTS before anything else
# tries to append hook commands into it. Actual hook content (e.g.
# lint-staged invocation) is a separate, later manual step — see
# lint-staged-hooks in checklist-rules-react-vite-ts.yaml and
# husky-pre-commit.template.sh.
